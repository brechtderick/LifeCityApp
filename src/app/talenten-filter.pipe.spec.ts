import { TalentenFilterPipe } from './talenten-filter.pipe';

describe('TalentenFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TalentenFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
