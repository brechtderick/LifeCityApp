import { LevenslijnFilterPipe } from './levenslijn-filter.pipe';

describe('LevenslijnFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LevenslijnFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
