import { HulpbronnenFilterPipe } from './hulpbronnen-filter.pipe';

describe('HulpbronnenFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HulpbronnenFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
