import { DoelFilterPipe } from './doel-filter.pipe';

describe('DoelFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DoelFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
